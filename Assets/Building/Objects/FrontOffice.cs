using UnityEngine;

public class FrontOffice : Tile
{
    // // Start is called once before the first execution of Update after the MonoBehaviour is created
    // void Start()
    // {
    //     anchor = true;
    //     isValid = true;
        
    //     // Front office always adds itself to the game
    //     BuildingGrid.Instance.SetAnchor(new Vector2Int(-2, 0), new Vector2Int(2, 3));
    // }
}
